// @potential/harness — IndexedDB persistence layer (Dexie) + Zustand session state
// Owns: RoomList append-only store, session state, compression triggers

export * from "./db/index.js";
export * from "./store/index.js";
