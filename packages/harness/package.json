{
  "name": "@potential/harness",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "type-check": "tsc --noEmit",
    "lint": "eslint src/",
    "test": "vitest run --passWithNoTests",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "fake-indexeddb": "^6.2.5",
    "typescript": "*",
    "vitest": "*"
  },
  "dependencies": {
    "@potential/shared": "workspace:*",
    "dexie": "^3.2.0",
    "zod": "^3.23.0",
    "zustand": "^4.5.0"
  }
}
